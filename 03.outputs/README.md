# Generated outputs

Figures and tables produced by the manuscript at render time. Committed, because the
manuscript and the root README both reference them as files rather than embedding them
twice.

`figures/` receives every plot at 300 dpi. `tables/` receives any table written to disk by
a pipeline block, as distinct from tables composed inside the document.
